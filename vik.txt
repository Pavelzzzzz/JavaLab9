контейнер должен содержать toString
распределить ответрсревнности
методы должны передавать только значения
обеспечить безопастность данных для кажлого класса сущности
@Before  просто перед каждым методом
@BeforeClass только одни раз перед всеми тестами

добавить set
классы util не тестить
переписать иерархию

таск
выьрать одну из трех коллекций: set(требуется уникальность), map(ключ), list(90)
класс necklace
